package com.jg.imagesearch.core.model

import androidx.compose.ui.graphics.Color

object AppColors {
    // Shimmer
    val ShimmerBase = Color(0xFFE0E0E0)
    val ShimmerHighlight = Color(0xFFF5F5F5)

    // Selection Overlay
    val SelectionOverlay = Color.Black.copy(alpha = 0.5f)
}
